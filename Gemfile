source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'docker-api' # for safely starting Ruby scripts in containers

# for GUI:
gem "puma"
gem "sinatra"
gem "sinatra-contrib"

# code formatting:
gem 'rubocop', require: false
